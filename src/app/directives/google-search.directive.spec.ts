import { GoogleSearchDirective } from './google-search.directive';

describe('GoogleSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleSearchDirective();
    expect(directive).toBeTruthy();
  });
});
